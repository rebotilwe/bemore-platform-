# BeMore Platform — Updates Log
**Date:** 29 April 2026
**Environment:** Production (bemore-tawny.vercel.app)

---

## 1. Polls & Mentee Meter — Full Removal

### What Changed
The entire polls/Mentee Meter feature was removed from both the frontend and backend. This includes the live poll voting system, SSE real-time updates, admin poll management interface, and the public Mentee Meter page.

### Backend — Deleted Files
| File | Description |
|------|-------------|
| `backend/src/routes/polls.js` | All poll API route definitions |
| `backend/src/controllers/pollController.js` | Poll CRUD, vote, SSE, status controllers |
| `backend/src/services/pollService.js` | Poll business logic + in-memory vote cache |
| `backend/src/services/pollSSE.js` | Server-Sent Events broadcast service |
| `backend/src/models/Poll.js` | Mongoose poll + question schema |
| `backend/src/models/PollResponse.js` | Mongoose vote response schema |
| `backend/__tests__/polls.test.js` | Poll API integration tests |
| `backend/__tests__/pollSSE.test.js` | SSE unit tests |

### Backend — Modified Files
| File | Change |
|------|--------|
| `backend/src/routes/index.js` | Removed polls router mount (`/polls`) and CSRF rule |
| `backend/__tests__/analytics.test.js` | Removed `Poll`/`PollResponse` imports and `poll.vote` seed event |

### Frontend — Deleted Files
| File | Description |
|------|-------------|
| `frontend/src/pages/admin/polls.ts` | Admin poll management page (list, builder, control panel, results) |
| `frontend/src/pages/public/mentee-meter.ts` | Public Mentimeter iframe embed page |
| `frontend/src/services/poll-sse.ts` | Client-side SSE EventSource wrapper |
| `frontend/src/components/poll-results-chart.ts` | Poll result visualisation components |
| `frontend/src/styles/pages/poll-admin.css` | Admin poll interface styles |
| `frontend/src/styles/pages/mentee-meter.css` | Public Mentee Meter page styles |

### Frontend — Modified Files
| File | Change |
|------|--------|
| `frontend/src/router.ts` | Removed `/mentee-meter` and `/admin/polls` routes |
| `frontend/src/api.ts` | Removed all poll API methods (`getPolls`, `createPoll`, `updatePoll`, `setPollStatus`, `setPollActiveQuestion`, `getPollResults`, `deletePoll`, `pollVote`, `getActivePoll`) |
| `frontend/src/store.ts` | Removed `pollsEnabled` state field from `AppState` |
| `frontend/src/main.ts` | Removed `polls_enabled` feature flag loading on app init |
| `frontend/src/pages/admin/layout.ts` | Removed "Mentee Meter" entry from admin sidebar |
| `frontend/src/pages/admin/settings.ts` | Removed `polls_enabled` and `mentimeter_id` setting definitions |
| `frontend/src/pages/admin/traffic.ts` | Removed `/mentee-meter` and `/admin/polls` from page name map |
| `frontend/src/components/nav.ts` | Removed conditional "Live Poll" nav link |
| `frontend/src/pages/public/landing.ts` | Removed "Join Live Session" CTA card |
| `frontend/src/pages/public/success.ts` | Removed "Live Poll" next-step card |
| `frontend/src/pages/public/status.ts` | Removed "Live Poll" next-step card |
| `frontend/src/styles/main.css` | Removed `@import` for poll and mentee-meter CSS |
| `frontend/src/__tests__/nav.test.ts` | Removed poll-related test cases |

### Test Results After Removal
- **Backend:** 215 / 215 tests passing
- **Frontend:** 183 / 183 tests passing

---

## 2. Service Worker — All Environments

### What Changed
The service worker was previously restricted to the production hostname only (`bemore-tawny.vercel.app`). It now registers on all environments including staging and local development.

### File Modified
`frontend/src/main.ts`

### Before
```typescript
const isProductionHost = window.location.hostname === 'bemore-tawny.vercel.app';
if (isProductionHost && 'serviceWorker' in navigator) {
  navigator.serviceWorker.register('/sw.js').catch(() => {});
} else if ('serviceWorker' in navigator) {
  // Unregister any existing SW in staging/dev to avoid stale cache issues
  navigator.serviceWorker.getRegistrations().then(regs => regs.forEach(r => r.unregister()));
}
```

### After
```typescript
// Register service worker for all environments
if ('serviceWorker' in navigator) {
  navigator.serviceWorker.register('/sw.js').catch(() => {});
}
```

### Service Worker Behaviour (unchanged)
The `public/sw.js` (cache name `bemore-static-v2`) uses stale-while-revalidate for static assets only — CSS, JS, fonts, icons. It never caches API calls, HTML navigation, or non-GET requests.

---

## 3. About Page — Tab-Based Routing

### What Changed
The About page was a single long-scroll page with 7 sections linked by a sticky sub-nav that smooth-scrolled to each section. Each tab is now a **fully independent page** with its own URL, rendering only that section's content.

### New Routes
| Route | Page |
|-------|------|
| `/#/about` | Overview (default) |
| `/#/about/overview` | Overview — Executive Summary |
| `/#/about/group` | Group Structure — HoldCo subsidiaries |
| `/#/about/vision` | Vision & Mission — Envisioning 2035 |
| `/#/about/empowerment` | Empowerment Strategy — HDI Inclusion Model |
| `/#/about/impact` | Impact to Date — Track Record 2013–2023 |
| `/#/about/performance` | Performance Metrics — Current Portfolio |
| `/#/about/opportunity` | The Opportunity — Strategic Rationale |

### Files Modified
| File | Change |
|------|--------|
| `frontend/src/pages/public/about.ts` | Rewritten using page factory pattern — one `makePage(tab)` function generates all 7 pages; each exports as a named export |
| `frontend/src/router.ts` | Registered 7 new routes + updated import |
| `frontend/src/components/nav.ts` | "About Us" link updated from `#/about` → `#/about/overview` |

### Page Structure (each tab page)
Every tab page renders the same hero banner and sticky sub-nav (with active tab highlighted in gold), followed by only that tab's section content, and ends with the "Ready to Partner?" CTA.

### Architecture Pattern
```
makePage('overview') → aboutOverviewPage
makePage('group')    → aboutGroupPage
...
```
All content lives in `SECTION_CONTENT` record keyed by tab. The sub-nav renders all 7 tabs as hash links, marking the current one `.active`.

---

---

## 4. Profile-Aware Multi-Step Form — Full Rewrite

### What Changed
All four specialist profile types now have fully tailored form experiences across all five steps — different field sets, labels, validation rules, and data structures per profile.

### Investor (Phase 1B)

**Step 2 — Investment Profile:**
- Sectors of interest (checkboxes): Student Accommodation, Residential, Commercial, Mixed-Use, Healthcare
- Investment amount (select): `<R5m` → `R100m+`
- Investment horizon (select): `<1 year` → `5+ years`
- Preferred return structure (checkboxes): Equity Stake, Profit Share, Interest-Bearing Loan, Combination
- Prior property investment experience (radio)

**Step 3 — Investment Intentions:**
- Seeking checkboxes: Co-Investment, Deal Flow, Fund Placement, Institutional JV
- Decision-making timeline (select): Immediate → Exploratory only

**Step 4 — Additional Notes:** Optional textarea (1,000 chars max, no minimum)

### Land Owner (Phase 1D)

**Step 2 — Land Profile:**
- Land size, zoning status, serviced radio, ownership structure
- Development Appetite radio: Develop myself / JV / Sell / Open to all
- Timeline select: Immediate → Exploratory
- Existing bond or mortgage radio

**Step 3 — Partnership Preferences:**
- Outcome checkboxes: Equity stake / Upfront proceeds / Rental income / Combination
- Recent land valuation radio

### Built Environment Professional (Phase 1A)

**Step 2 — Professional Profile:**
- Profession/Discipline: 10 options (Architect, Civil/Structural Engineer, QS, PM, Town Planner, MEP, Interior Designer, Other)
- Registration Body: SACAP, ECSA, ASAQS, SACPCMP, SACPLAN, Not yet registered, Other
- Optional registration/professional number
- Years of experience + typical project value
- Active provinces (9 province checkboxes)

**Step 3 — Association Interest:**
- Willingness to join associate database (3-option radio)
- Current capacity for new projects (4-option radio)
- Preferred work types (6 checkboxes)
- Brief motivation textarea (optional, 500 chars)

**Step 4 — Additional Information:** Optional textarea (1,000 chars max)

### Student Accommodation Operator (Phase 1C)

**Step 2 — Portfolio Profile:**
- Total bed count + average occupancy (portfolio-wide)
- Active provinces (9 province checkboxes)
- Primary city/town (text input)
- Asset type (added Mixed Portfolio) + asset ownership structure
- NSFAS accreditation status (Fully / Provisionally / Pending / Not accredited)
- University partnership (Yes / No / In negotiation)

**Step 3 — Support Needs:**
- Support type checkboxes (6): management, NSFAS compliance, capital raise, bed placement, JV/partnerships, PropTech
- Growth intention radio (Grow significantly / Moderate / Maintain / Exploring)
- Previous institutional funding radio

**Step 4 — Portfolio Description:** Required textarea (min 50 chars)

### Profile-Aware Step Labels
`form-steps.ts` now exports `getStepMeta(stepIndex, profile)` which merges base step metadata with profile-specific overrides:

| Profile | Step 2 | Step 3 | Step 4 |
|---------|--------|--------|--------|
| developer | Readiness | Funding | Project |
| investor | Profile | Intentions | Notes |
| landowner | Land | Partnership | Project |
| professional | Profile | Interest | Notes |
| student | Portfolio | Needs | Details |

### Files Modified
| File | Change |
|------|--------|
| `frontend/src/pages/public/form-steps/step-readiness.ts` | Rewrote all 4 profile field blocks; added `SKIP_GENERIC` set (investor, professional, student skip generic land/stage/value) |
| `frontend/src/pages/public/form-steps/step-funding.ts` | Added `renderInvestor()`, `renderLandowner()`, `renderProfessional()`, `renderStudent()`; dispatched from `renderStepFunding(profile)` |
| `frontend/src/pages/public/form-steps/step-project.ts` | Profile-aware: investor and professional get optional textarea; student gets required portfolio description |
| `frontend/src/constants/form-steps.ts` | Added `getStepMeta()` with `INVESTOR_OVERRIDES`, `LANDOWNER_OVERRIDES`, `PROFESSIONAL_OVERRIDES`, `STUDENT_OVERRIDES` |
| `frontend/src/pages/public/form.ts` | Validation branches for steps 2, 3, 4 per profile; `collectAllFormData()` captures correct fields per profile |

---

## 5. Backend Schema & Auto-Tag Updates

### Application Model
- Added `allocatedProjects: [String]` field — admin-assignable project references for professional applicants

### Sanitize Update Allowlist
`applicationService.js` `ALLOWED_UPDATE_FIELDS` now includes `allocatedProjects`

### Auto-Tag Engine (`autoTag.js` + `auto-tag.ts`)
Updated field names and new tags for all four updated profiles:

| Profile | New / Updated Tags |
|---------|--------------------|
| investor | `LARGE_INVESTOR` (via `investmentAmount`), `HIGH_VALUE`, `EXPERIENCED`, `STUDENT_FOCUS`, `PIPELINE_READY` (immediate timeline) |
| student | `LARGE_OPERATOR` / `HIGH_OCCUPANCY` (via `totalBedCount` / `averageOccupancy`), `NSFAS_ACCREDITED`, `PIPELINE_READY` (growth intent), `FUNDING_STAGE` (support needs) |
| professional | `LARGE_SCALE` (via `typicalProjectValue`), `REGISTERED` (via `registrationBody`: SACAP/ECSA/ASAQS), `PIPELINE_READY` (immediately available), `SEEKS_EQUITY` (actively seeking) |
| landowner | `SEEKS_EQUITY` (JV/open appetite), `PIPELINE_READY` (no existing bond) |

### Lookup Route
`POST /api/applications/lookup` now returns `allocatedProjects` in the response payload

---

## 6. Admin Dashboard Updates

### App Detail Modal (`app-detail-modal.ts`)
- `renderProfileFields()` fully rewritten for all 4 profiles with complete field coverage
- Generic "Readiness Assessment" and "Funding Requirements" sections only render for `developer` and `aspiring` profiles
- Professionals get an **Allocated Projects** panel at the bottom of the modal
- Investor fields: Investment Profile section + Investment Intentions section + Additional Notes
- Student fields: Portfolio Profile section + Support Needs section + Portfolio Description
- Professional fields: Professional Profile section + Association Interest section

### Leads Cards (`leads.ts`)
Added `getQuickInfo(userType, fd)` — surfaces the most relevant headline stat per profile on each card:

| Profile | Quick Info shown |
|---------|-----------------|
| developer / aspiring | Estimated project value |
| investor | Investment amount |
| landowner | Land size · Zoning status |
| student | Total bed count · Average occupancy |
| professional | Profession · Typical project value |

---

## 7. Status Page — Allocated Projects

### What Changed
Professional applicants now see an **Allocated Projects** section on the status page after their intelligence tags.

- If projects have been assigned by the admin, they appear as a list with gold chevron icons
- If no projects are allocated yet, a friendly message is shown: *"Our team will be in touch once a suitable opportunity arises."*

### Files Modified
| File | Change |
|------|--------|
| `backend/src/routes/applications.js` | Lookup route selects and returns `allocatedProjects` |
| `frontend/src/api.ts` | `lookupStatus` return type extended with `allocatedProjects: string[]` |
| `frontend/src/pages/public/status.ts` | Conditionally renders Allocated Projects section for professionals |
| `frontend/src/styles/pages/status.css` | Added `.st-allocated-*` styles (gold-bordered panel, item list, empty state) |

---

## 8. Test Coverage

### Frontend (auto-tag.test.ts)
Rewrote investor, student, and professional test cases to use new field names; added 13 new test cases covering all new tags.

| Suite | Before | After |
|-------|--------|-------|
| Frontend tests | 183 | 196 |
| Backend tests | 215 | 215 |

---

## 9. API Hardening & Security Polish

### CSRF Protection Fix
The CSRF double-submit middleware (`auth.js`) had a bypass condition — when the `bm_csrf` cookie was absent, the old check `if (cookieToken && cookieToken !== headerToken)` silently passed. Fixed to explicitly reject missing cookies with 403.

### API Response Contract Fixes
| Controller | Issue | Fix |
|------------|-------|-----|
| `trafficController.js` | Error responses used `error:` field (inconsistent with envelope) | Changed all 7 error responses to `message:` |
| `analyticsController.js` | `eventLog` spread `{ ...data }` wrapping events + pagination at root | Destructured to `{ data: events, pagination }` — correct shape |

### Dead Settings Keys Removed
`ALLOWED_SETTINGS` and `PUBLIC_SETTINGS` cleaned of keys that no longer exist in the codebase:

| Key removed | Reason |
|-------------|--------|
| `mentimeterEmbedId` | Removed with Mentee Meter feature |
| `mentimeter_id` | Removed with Mentee Meter feature |
| `polls_enabled` | Removed with Polls feature |

`summit_config` added to `PUBLIC_SETTINGS` (was missing).

### OpenAPI Spec Cleanup
- Removed 350-line polls section (`/polls/*` endpoints, `Polls` tag, Vote rate limit row)
- Updated `PUT /settings/{key}` enum to `[registrationOpen, maintenanceMode, platformAnnouncement, summit_config]`
- Updated public settings description and auto-tag list

### Test Coverage
All fixes covered by existing test suite — both popia-settings and analytics tests updated to match corrected API contracts.

| Suite | Tests |
|-------|-------|
| Frontend | 196 / 196 passing |
| Backend | 215 / 215 passing |

---

## Commits

| Hash | Message |
|------|---------|
| `5cac1c1` | feat: remove polls/mentee-meter, enable SW for all environments |
| `d39c7f3` | fix: remove remaining poll/mentee-meter references from traffic and settings pages |
| `558f5c4` | feat: convert about page tabs to individual routes |
| `69a6130` | feat(form): Phase 1B+1D — Investor and Land Owner form rewrites |
| `a9713ab` | feat(form): Phase 1A — Built Environment Professional form rewrite |
| `1592299` | feat(form): Phase 1C — Student Accommodation Operator form rewrite |
| `5632f9b` | feat(backend+admin): Phase 2 & 3 — schema, auto-tag, and admin dashboard updates |
| `26a7b6d` | feat(status+tests): Phase 4 & 5 — allocated projects, test coverage updates |
| `0199702` | docs: update architecture, CLAUDE.md, and updates log |
| `a122689` | fix: hardening pass — CSRF, API contracts, dead settings keys, OpenAPI cleanup |
| `ecdb202` | chore: add .railwayignore for clean railway up deploys |

---

## Deployment

All changes deployed and verified against live endpoints on both environments.

| Environment | URL | Status | Backend | MongoDB |
|-------------|-----|--------|---------|---------|
| Production | https://bemore-tawny.vercel.app | Live | bemore-production.up.railway.app | Connected |
| Staging | https://bemorecapital.co.za | Live | bemore-staging.up.railway.app | Connected |

### Endpoint Verification (29 Apr 2026)
| Check | Result |
|-------|--------|
| `GET /api/health` | `database: ok` on both environments |
| `GET /api/settings/public/mentimeter_id` | 404 — key correctly blocked |
| `GET /api/settings/public/polls_enabled` | 404 — key correctly blocked |
| `GET /api/settings/public/summit_config` | 200 — `active: false` |
| `GET /api/settings/public/registrationOpen` | 200 |
| `POST /api/applications` (professional) | Submit successful, ref generated |
| `POST /api/applications` (student) | Submit successful, `UNI_ACCREDITED` tag applied |
| `POST /api/applications/lookup` | Returns `allocatedProjects: []` for new applicants |
